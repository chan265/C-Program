

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int num;
    long long product = 1;
    scanf("%d", &num);
    for (int i = 1; i <= num; i++) 
    {
        product *= i;
    }
    printf("Product of value is %lld\n", product);

    return 0;
}
